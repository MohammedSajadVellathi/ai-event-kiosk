# RunPod ComfyUI instance URL — get this from your RunPod pod's proxy URL
COMFYUI_URL=https://YOUR_POD_ID-8188.proxy.runpod.net

# Base URL of this backend server (used in image URLs returned to frontend)
API_BASE_URL=http://localhost:8000
